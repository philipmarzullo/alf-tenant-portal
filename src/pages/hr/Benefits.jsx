import EmptyState from '../../components/shared/EmptyState';

export default function Benefits() {
  return <EmptyState title="Benefits Enrollment" description="Kanban board with enrollment pipeline coming in next build step." />;
}
